t = int(input())

for i in range(1, t+1):
    R, C = [int(s) for s in input().split(' ')]
    # print(R, C)
    # possible_chars = {}
    possible_chars = set()
    lines = []
    cake = ''
    i = 0
    # idx = []
    for _ in range(R)
        line = input()
        cake += line
        for c in line:
            if c != '?': possible_chars.add(c)
            # else: 
            i += 1
    
    for i, c in enumerate(cake):
        row = i // 4
        col = i % 4
        if c == '?':
            c == i
            
            

        


    ans = ''

    print('Case #{}:'.format(i))
